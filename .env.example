# Local dev: scrape DB read at build time
DB_PATH=/home/tharaka/projects/flixpatrol/data/flixpatrol.db

# CI / Cloudflare Pages (encrypted env vars in dashboard, never committed):
# CLOUDFLARE_API_TOKEN=<token with D1 read+write>
# CLOUDFLARE_ACCOUNT_ID=<account id>
# Pages build command: npm run build:ci   (exports D1 -> .d1/ then builds)
# Daily refresh: scrape --save, then `npm run db:push`, then hit the Pages deploy hook.
